{
  "name": "@sentinel/ai",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-grounded AI explanation layer + PRISM observability adapter for Sentinel",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types --no-warnings \"tests/**/*.test.ts\"",
    "typecheck-tests": "tsc -p tests/tsconfig.json --noEmit",
    "check": "npm run build && npm run typecheck && npm run typecheck-tests && npm run test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
